using System;

class SampleApplication{
    static void Main(){
        Console.WriteLine("Hello World!");
    }
}























//specifies to the console. Live JS, semicolon used to end specific instruction
//  Console.WriteLine("Hello World!");

//Main namespace of the Base Class Library (a vast library code that provides the most basic functionality of C# language)
//  System

//Keyword that tells the compiler that I'll be using the System library
//  using

//instructs compiler that anythin between curly braces is a part of class named SampleApplication. A class is an organized grouping of code
//  class SampleApplication

//Function that tell the compiler to execute anything between curly braces. In a console application, a function with the name
    //Main() is automatically executed as the first step running in the program
//  Main()

//Keyword used as a type of modifier for the main() function. it refers to itself rather than creating an instance of class it's modifying.
    //It tells the compiler that this function can be called without relying on reference to an object
//  static

//Another modifier of the Main() function. It tells the compiler that this function isn't going to return a value. Instead,
    //it's just going to perform an action (like printing words to console)
//  void

